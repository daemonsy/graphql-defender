require 'spec_helper'

describe Graphql::Defender do
  it 'has a version number' do
    expect(Graphql::Defender::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
